---
# Generated by WCA for GP
- name: Get events from a Kubernetes pod
  hosts: all
  tasks:
    - name: Get events from a Kubernetes pod
      shell: kubectl get events --field-selector involvedObject.name={{ pod_name }} --namespace {{ namespace }}
      register: events

    - name: Print events
      debug:
        msg: "{{ events.stdout }}"
